services:
  - type: web
    name: detea-reddit-proxy
    runtime: node
    plan: free
    rootDir: express-backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
