# Render — Dart backend (replaces backend-vertex Node service)
# Set Root Directory to `server` in Render dashboard.
services:
  - type: web
    name: socitea-backend
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
