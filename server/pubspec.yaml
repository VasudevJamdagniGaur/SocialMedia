name: deite_server
description: Deite unified Dart backend — Vertex AI, NewsAPI proxy, LinkedIn, cron ingest.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  dotenv: ^4.2.0
  googleapis_auth: ^1.6.0
  html: ^0.15.4
  http: ^1.2.2
  shelf: ^1.4.2
  shelf_cors_headers: ^0.1.5
  shelf_router: ^1.1.4

dev_dependencies:
  lints: ^5.0.0
