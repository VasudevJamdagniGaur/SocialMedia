# Google Generative AI API Key
# Get your API key from: https://makersuite.google.com/app/apikey
REACT_APP_GOOGLE_API_KEY=your_google_api_key_here

# LinkedIn OAuth (from LinkedIn Developer Portal → your app → Auth)
LINKEDIN_CLIENT_ID=your_linkedin_client_id_here
LINKEDIN_CLIENT_SECRET=your_linkedin_client_secret_here

# NewsAPI — get a key: https://newsapi.org/register
# For `npm start` / localhost: `src/setupProxy.js` reads this (not bundled to the client):
# NEWSAPI_KEY=your_newsapi_key_here
#
# CRA bundle / production build (embedded in the app):
REACT_APP_NEWSAPI=your_newsapi_key_here
#
# Android APK: set REACT_APP_NEWSAPI here (or in CI env) before `npm run build` — CRA embeds it in the bundle.
# Then: npx cap sync android and rebuild the APK. Without this, direct NewsAPI calls have no key on device.
#
# Alternatively, use Firebase backend only: the WebView loads https://localhost — browser fetch to NewsAPI can fail CORS.
# Set the key on the server:
#   Firebase Console → Build → Functions → your project → Configuration → Environment variables
#   Add NEWSAPI_KEY (or REACT_APP_NEWSAPI) = your NewsAPI key, then deploy: firebase deploy --only functions,hosting
#
# Optional: if your Hosting URL is not deitedatabase.web.app, set it here (no trailing slash):
# REACT_APP_NEWS_PROXY_ORIGIN=https://your-project.web.app

# Backend-only option: call Cloud Function `newsApi` directly (bypasses Hosting rewrites).
# Set this to the exact deployed URL of the function, then rebuild.
# REACT_APP_NEWSAPI_FUNCTION_URL=https://<region>-<project>.cloudfunctions.net/newsApi

# World News API — https://worldnewsapi.com/
# REACT_APP_WORLDNEWS_API_KEY=your_worldnews_api_key_here
# REACT_APP_WORLDNEWS_API_2=your_second_worldnews_api_key_here
# WORLDNEWS_API_KEY=your_worldnews_api_key_here

# GNews — https://gnews.io/
# REACT_APP_GNEWS_API_KEY=your_gnews_api_key_here
# REACT_APP_GNEWS_API_2=your_second_gnews_api_key_here
# GNEWS_API_KEY=your_gnews_api_key_here

# Thenews API — API tokens (provider URL in code when integrating)
# REACT_APP_THENEWS_API_TOKEN=your_thenews_api_token_here
# REACT_APP_THENEWS_API_2=your_second_thenews_api_token_here
# THENEWS_API_TOKEN=your_thenews_api_token_here
